import React from 'react';
import './Header.css';

const Header = () => (
  <header className="header">
    <div className="header-profile">whatever the header is</div>
  </header>
);

export default Header;
